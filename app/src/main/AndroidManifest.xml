<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoskControl"
        tools:targetApi="31">

        <!--
          核心：MainActivity是唯一的Activity。
          它的intent-filter同时包含了：
          1. action.MAIN: 声明它是应用的主入口。
          2. category.HOME: 声明它可以作为一个桌面/主屏幕。
          3. category.DEFAULT: 必须与HOME一起使用，让系统可以找到它。
          这三者结合，就实现了“开机直接进入此APP”的功能。
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>